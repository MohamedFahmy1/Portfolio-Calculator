rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /portfolios/default {
      allow read, write: if request.auth != null
        && request.auth.token.email == "engmohamedmahmoud1997@gmail.com";
    }
  }
}
